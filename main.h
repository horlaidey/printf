#ifndef MAIN_H
#define MAIN_H
#include <stdio.h>
#include <unistd.h>
#include <stdarg.h>
int _putchar(char);
int _printf(const char *format, ...);
int _strlen(char *);
int _putint(int);
int _putuint(unsigned int);
int _binary(unsigned int);



#endif
